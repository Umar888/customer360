import 'package:flutter/material.dart';

class AddCommissionLogScreen extends StatelessWidget {
  AddCommissionLogScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
